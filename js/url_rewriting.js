switch (window.location.hash) {
  case "#about":
    loadAboutContent();
    break;
  case "#contact":
    loadContactContent();
    break;
  // Add more cases for each virtual URL
}

function loadAboutContent() {
  // Load the content for the about page
}

function loadContactContent() {
  // Load the content for the contact page
}
